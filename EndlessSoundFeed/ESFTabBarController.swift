//
//  ESFTabBarController.swift
//  EndlessSoundFeed
//
//  Created by Jean-Marc Boullianne on 5/2/17.
//  Copyright © 2017 Jean-Marc Boullianne. All rights reserved.
//

import UIKit

class ESFTabBarController: UITabBarController {

    override func viewDidLoad() {
        super.viewDidLoad()

        // Do any additional setup after loading the view.
        /*
        let npBar = UIView()
        self.view.addSubview(npBar)
        
        npBar.backgroundColor = UIColor(red: 0, green: 0, blue: 0, alpha: 0.3)
        
        npBar.translatesAutoresizingMaskIntoConstraints = false
        npBar.leadingAnchor.constraint(equalTo: self.tabBar.layoutMarginsGuide.leadingAnchor).isActive = true
        npBar.heightAnchor.constraint(equalToConstant: 45).isActive = true
        npBar.bottomAnchor.constraint(equalTo: self.tabBar.layoutMarginsGuide.topAnchor).isActive = true
        npBar.trailingAnchor.constraint(equalTo: self.tabBar.layoutMarginsGuide.trailingAnchor).isActive = true
        
        let label = UILabel(frame: npBar.frame)
        label.text = "No Media Playing"
        label.textColor = UIColor.white
        npBar.addSubview(label)
        */
        
        
        
        
        
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }
    

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destinationViewController.
        // Pass the selected object to the new view controller.
    }
    */

}
